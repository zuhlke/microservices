---
applications:
- name: edge-service
  memory: 512M
  instances: 1
  host: edge-service-${random-word}
  domain: cfapps.io
  path: target/edge-service.jar
  services:    
    - config-service
    - discovery-service
  env:
    SPRING_PROFILES_ACTIVE: cloud
    DEBUG: "true"
